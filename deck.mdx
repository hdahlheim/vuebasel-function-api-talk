import {
  CodeSurferLayout,
  CodeSurferColumnLayout,
} from "code-surfer"
import {
  future
} from 'mdx-deck/themes'
import { oceanicNext } from "code-surfer"

export const themes = [ oceanicNext, future ]

# A look at the
# Function API RFC

---

# Hi 👋,
# I’m Henning

---


<CodeSurferLayout>

```jsx title="React Component without hooks"
import React from 'react'

export default class MyComponent extends React.Component {
  constructor(props) {
    super(props)
    this.state = { count: 0 };
  }
  incremnet () {
    this.setState({ count: this.state.count + 1 })
  }
  render () {
    return (
      <div>
        <p>You clicked {this.state.count} times</p>
        <button
          onClick={this.incremnet}
        >Click me</button>
      </div>
    )
  }
}
```

```jsx 4:9
import React from 'react'

export default class MyComponent extends React.Component {
  constructor(props) {
    super(props)
    this.state = { count: 0 };
  }
  incremnet () {
    this.setState({ count: this.state.count + 1 })
  }
  render () {
    return (
      <div>
        <p>You clicked {this.state.count} times</p>
        <button
          onClick={this.incremnet}
        >Click me</button>
      </div>
    )
  }
}
```

```jsx 11:20
import React from 'react'

export default class MyComponent extends React.Component {
  constructor(props) {
    super(props)
    this.state = { count: 0 };
  }
  incremnet () {
    this.setState({ count: this.state.count + 1 })
  }
  render () {
    return (
      <div>
        <p>You clicked {this.state.count} times</p>
        <button
          onClick={this.incremnet}
        >Click me</button>
      </div>
    )
  }
}
```

</CodeSurferLayout>

---


<CodeSurferLayout>

```jsx title="React component with hooks"
import React, { useState } from 'react'

function Example() {
  const [count, setCount] = useState(0)
  const increment = () => setCount(count + 1)

  return (
    <div>
      <p>You clicked {count} times</p>
      <button onClick={increment}>
        Click me
      </button>
    </div>
  )
}
```

```js 4:5
import React, { useState } from 'react'

function Example() {
  const [count, setCount] = useState(0)
  const increment = () => setCount(count + 1)

  return (
    <div>
      <p>You clicked {count} times</p>
      <button onClick={increment}>
        Click me
      </button>
    </div>
  )
}
```

</CodeSurferLayout>

---

<CodeSurferLayout>

```jsx title="Vue 2 component"
<template>
  <div>
    <p>You clicked {{ count }} times</p>
    <button @click="increment">Click me</button>
  </div>
</template>

<script>
export default {
  data () {
    return {
      count: 0
    }
  },
  methods:{
    increment () {
      this.count++
    }
  }
}
</script>
```

```jsx 10:14
<template>
  <div>
    <p>You clicked {{ count }} times</p>
    <button @click="increment">Click me</button>
  </div>
</template>

<script>
export default {
  data () {
    return {
      count: 0
    }
  },
  methods:{
    increment () {
      this.count++
    }
  }
}
</script>
```

```jsx 14:18
<template>
  <div>
    <p>You clicked {{ count }} times</p>
    <button @click="increment">Click me</button>
  </div>
</template>

<script>
export default {
  data () {
    return {
      count: 0
    }
  },
  methods:{
    increment () {
      this.count++
    }
  }
}
</script>
```

```jsx 1:22
<template>
  <div>
    <p>You clicked {{ count }} times</p>
    <button @click="increment">Click me</button>
  </div>
</template>

<script>
export default {
  data () {
    return {
      count: 0
    }
  },
  methods:{
    increment () {
      this.count++
    }
  }
}
</script>
```

</CodeSurferLayout>

---

<CodeSurferLayout>

```jsx title="Vue 3 component with setup"
<template>
  <div>
    <p>You clicked {{ count }} times</p>
    <button @click="increment">Click me</button>
  </div>
</template>

<script>
import { value } from 'vue'
export default {
  setup () {
    const count = value(0)
    const increment = () => count.value++
    return {
      count,
      increment
    }
  }
}
</script>
```

```jsx 9
<template>
  <div>
    <p>You clicked {{ count }} times</p>
    <button @click="increment">Click me</button>
  </div>
</template>

<script>
import { value } from 'vue'
export default {
  setup () {
    const count = value(0)
    const increment = () => count.value++
    return {
      count,
      increment
    }
  }
}
</script>
```

```jsx 11:13
<template>
  <div>
    <p>You clicked {{ count }} times</p>
    <button @click="increment">Click me</button>
  </div>
</template>

<script>
import { value } from 'vue'
export default {
  setup () {
    const count = value(0)
    const increment = () => count.value++
    return {
      count,
      increment
    }
  }
}
</script>
```

```jsx 14:17
<template>
  <div>
    <p>You clicked {{ count }} times</p>
    <button @click="increment">Click me</button>
  </div>
</template>

<script>
import { value } from 'vue'
export default {
  setup () {
    const count = value(0)
    const increment = () => count.value++
    return {
      count,
      increment
    }
  }
}
</script>
```

```jsx 1:22
<template>
  <div>
    <p>You clicked {{ count }} times</p>
    <button @click="increment">Click me</button>
  </div>
</template>

<script>
import { value } from 'vue'
export default {
  setup () {
    const count = value(0)
    const increment = () => count.value++
    return {
      count,
      increment
    }
  }
}
</script>
```

</CodeSurferLayout>

---

<CodeSurferColumnLayout>

```jsx
<template>
  <div>
    <p>You clicked {{ count }} times</p>
    <button @click="increment">Click me</button>
  </div>
</template>

<script>
import { value } from 'vue'
export default {
  setup () {
    const count = value(0)
    const increment = () => count.value++
    return {
      count,
      increment
    }
  }
}
</script>
```
</CodeSurferColumnLayout>

<CodeSurferColumnLayout>

```jsx
<template>
  <div>
    <p>You clicked {{ count }} times</p>
    <button @click="increment">Click me</button>
  </div>
</template>

<script>
import { value } from 'vue'
export default {
  setup () {
    const count = value(0)
    const increment = () => count.value++
    return {
      count,
      increment
    }
  }
}
</script>
```

</CodeSurferColumnLayout>

---
